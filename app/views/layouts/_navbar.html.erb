<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">조인디</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li<%= ' class=active' if params[:controller] == 'home' %>><%= link_to('홈', '/') %></li>
        <li<%= ' class=active' if params[:controller] == 'events' and not params[:action] == 'new' %>><%= link_to('연합공연 정보', '/events') %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if team_signed_in? %>
          <li<%= ' class=active' if params[:controller] == 'events' and params[:action] == 'new' %>><%= link_to('연합공연 만들기', '/events/new') %></li>
          <li><%= link_to('로그아웃', '/teams/sign_out') %></li>
        <% else %>
          <li><%= link_to('로그인', '/teams/sign_in') %></li>
          <li><%= link_to('가입하기', '/teams/sign_up') %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>